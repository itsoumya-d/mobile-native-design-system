# Audit Severity

`proven` means a deterministic static check observed a concrete source signal.
`heuristic` means a reviewer should inspect the rendered screen before changing
it. Prioritize broken actions, inaccessible controls, clipping, loss of state,
and native navigation regressions before visual refinement.
